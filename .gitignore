# ── hako engine repo: engine source only ──

# Engine build artifacts
hakm
test_quant
test_api
*.o
*.a

# Model weights — never in the repo (pulled from HuggingFace at runtime)
*.mlf2
*.gguf
*.mlf
*.bin
*.pt
*.safetensors

# Model production: recipes + scratch kept LOCAL, not published here.
# (sho/ koi/ fine-tune recipes move to their own repo when training starts;
#  experimental/ is from-scratch research.)
sho/
koi/
experimental/
data/

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
.venv/
.ruff_cache/

# macOS
*.DS_Store

# Editors
.idea/
.vscode/
*.swp
